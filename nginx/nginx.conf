# ============================================================
# NOT USED by this project's dev or production stack.
#
# Both local dev and the droplet run Apache (see apache/000-default.conf and
# DROPLET.md). The site is fully static, so this config would serve it fine —
# it's kept only as an alternative reference for anyone who prefers nginx.
# ============================================================

worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent"';
    access_log /var/log/nginx/access.log main;
    error_log  /var/log/nginx/error.log warn;

    sendfile on;
    keepalive_timeout 65;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/javascript text/javascript;
    gzip_min_length 1024;

    server {
        listen       80;
        server_name  _;

        root  /usr/share/nginx/html;
        index index.html;

        # Cache static assets aggressively, but not HTML (so overrides work immediately)
        location ~* \.(css|js)$ {
            expires 1h;
            add_header Cache-Control "public, max-age=3600";
        }

        location / {
            try_files $uri $uri/ /index.html;
            add_header Cache-Control "no-cache";
        }
    }
}
