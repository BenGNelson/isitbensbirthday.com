services:
  web:
    build: .
    ports:
      - "${HOST_PORT:-8080}:80"
    volumes:
      # Hot-reload: mount site/ so HTML/CSS/JS edits are served immediately
      # without rebuilding the image. Remove for a production-style build.
      - ./site:/var/www/html:ro
    restart: unless-stopped
